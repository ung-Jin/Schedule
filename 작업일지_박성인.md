작업일지 — 냉·난방 AS기사 스케줄 관리 시스템 (Schedule)
========================================================

PPT 발표자료(`Schedule-발표.pptx`) 첨부용 참고 문서
작성자: 박성인 (담당 파트: **기사 대시보드 · AS결과보고**, 부가로 만족도조사 · SMS 연동 · 담당 화면 디자인)
프로젝트 기간: 2026-09-08 ~ 2026-09-15 (총 6 작업일, 주말 09-12~09-13 제외 / 학원 프로젝트)
핵심 목표: 사용자 → 관리자 → 기사 → 사용자로 이어지는 AS 요청 데이터 순환 구조 완성

> 이 문서는 팀 전체 진행 상황을 날짜순으로 정리하되, **본인이 직접 커밋한 부분(기사 대시보드 · AS결과보고 · 만족도조사 · SMS · 담당 화면 디자인)은 상세히**, 팀원 파트는 맥락 파악에 필요한 만큼만 간단히 적었습니다.

<br>

## 팀 구성 및 역할 분담

화면 단위로 담당을 나눠서, 백엔드/프론트를 나누지 않고 각자 화면 하나를 처음부터 끝까지 완성하는 방식으로 진행했습니다.

| 담당 화면 | 담당자 | 주요 내용 |
|---|---|---|
| 공통 · 로그인 · 통합 | 팀장 | 메인/로그인 화면, 공통 레이아웃, 권한별 화면 분기, 전체 통합 |
| 관리자 대시보드 · 기사관리 | 최준영 | 상태별 통계 카드, 최근 AS 접수 리스트, 기사 등록·수정·삭제 |
| 관리자 일정관리 | 김성준 | 캘린더 출력, 기사 배정, 일정 충돌 확인 |
| **기사 대시보드 · AS결과보고** | **박성인 (본인)** | 기사 본인 일정 확인, 방문 결과 등록(사진 포함), 만족도조사·SMS 연동 |

<br>

## Day 1 (2026-09-08). 프로젝트 셋업

목표: 저장소 생성, DB/화면 설계 확정, 역할 분담

* 리포지토리 최초 생성, Git 브랜치 전략 확정: `main`(최종 배포) / `dev`(통합 개발) / `feature/*`(개인 작업, 예: `ft/admin-engineer`)
* 협업 도구 결정: GitHub(코드) · 노션(문서) · ERDCloud(ERD) · 카카오톡(소통)
* DB 설계 초안(ERDCloud): `MEMBER` / `ENGINEER` / `REQUEST` / `AS_SCHEDULE` / `AS_RESULT` 5개 테이블 확정 (만족도조사 기능은 이후 Day 4에서 `SATISFACTION` 테이블로 별도 추가)
* Role 3종 확정: `admin`(관리자) / `repairman`(기사) / `user`(신청 고객) — `MEMBER.ROLE` 컬럼 ENUM으로 관리
* 화면 단위 역할 분담표 확정 (위 표 참고)

<br>

## Day 2 (2026-09-09). 로그인 + 화면 뼈대 + 결과보고 초기 구현

목표: 인증 기능 완료, 각자 담당 화면 뼈대 착수

**팀 전체**
* `html` 폴더 구조 및 공통 레이아웃(`common/layout.html`, `side.html`) 초안
* 로그인/역할별(role) 화면 분기 기능 완료
* 기사관리 패키지 골격 (controller/DTO/service/mapper) 생성

**본인 작업 (기사 대시보드 · AS결과보고)**
* `AS_RESULT` 테이블 연동 시작 — 기사가 방문 후 처리 내용/결과를 등록하는 **결과보고 화면** 초안 구현 (`기사 결과보고_v1`, `a.s기사 결과보고_v1`)
* 결과보고 화면을 v2로 개선 (`결과보고 페이지_v2`) — 처리 내용 입력, 사진 첨부(`resultImage`, `resultImage2` 최대 2장) 필드 구성
* 병합 과정에서 실수로 삭제된 SQL 시드 파일 복구

<br>

## Day 3 (2026-09-10). 기사 대시보드 본격 구현

목표: "오늘의 일정" 대시보드와 결과보고 기능을 하나의 화면 흐름으로 연결

**본인 작업**
* `AS_SCHEDULE` + `REQUEST` 조인 쿼리(`selectToday`)로 로그인한 기사의 **오늘 배정된 일정만** 필터링해서 보여주는 대시보드 리스트 구현 (`대시보드 및 결과보고 화면 및 기능 구현_v1`)
* 일정 카드 클릭 → 결과보고 화면으로 이동, "진행예정" 버튼 클릭 → 상태를 `IN_PROGRESS`로 변경하는 흐름 완성
* 팀 다른 파트(관리자 대시보드, 로그인 최적화 등)와 병합, 화면 간 라우팅 정합성 확인 (`대시보드 화면 병합_v1`)

**팀 전체**: 관리자 대시보드 기능 구현 완료, 메인 페이지 이미지/레이아웃 추가, 로그인 SQL 연동 및 role별 최적화

<br>

## Day 4 (2026-09-11). SMS 발송 + 만족도조사 기능 구현 (+ 버그 수정)

목표: 결과보고 등록 → 고객 문자 발송 → 만족도조사 응답까지 이어지는 순환 구조 완성

* Solapi SMS API 연동 — 기사가 결과보고를 등록하면(`REQUEST.STATUS`를 `COMPLETED`로 변경) 고객에게 만족도조사 링크를 문자로 자동 발송 (`sms 발송 기능 구현_v1`)
* 만족도조사를 구글폼이 아닌 **자체 서버(`/survey`) 페이지로 직접 받도록 설계** — 구글폼 응답은 DB로 안 들어와서 대시보드 통계에 실시간 반영이 안 되는 문제 때문에 자체 구현으로 방향 결정
* `SATISFACTION` 테이블 신규 설계 (방문/시간준수, 친절도/응대, A/S 처리결과 3개 항목 + 후기 코멘트)

**발견한 문제 → 수정**
* **문제**: SMS에 담을 만족도조사 링크에 기사 번호를 넣어야 하는데, 로그인 세션의 `MEMBER.MEM_NO`를 그대로 `ENGINEER.ENGINEER_NO` 자리에 넣어 보내면서 "기사번호 미조회" 오류 발생. `MEMBER`(회원)와 `ENGINEER`(기사)는 PK가 서로 다른 별개 테이블인데 이를 혼동한 것이 원인.
* **수정**: `MEM_NO`로 `ENGINEER` 테이블을 다시 조회해서 진짜 `ENGINEER_NO`로 변환하는 로직(`selectEngineerNo`)을 추가해 해결 (`만족도 조사 발송 수정 중_v1(기사번호 미조회 오류 해결 중)`)
* 오류 여부 전체 재테스트 진행 (`오류여부테스트중`)

<br>

## Day 5 (2026-09-14). 만족도 통계 반영 + 담당 화면 디자인 (가장 바쁜 날)

목표: 대시보드에 만족도 통계 시각화 + 담당 화면 전체 디자인 정리

* `SatisfactionService.getStats()` 구현 — 이번 달 평균 / 작년 동월 평균 / 증감 / 올해 1월~이번 달 월별 추이를 한 번에 계산해서 대시보드에 전달
* 대시보드에 "고객만족도 현황" 카드 추가: 이번 달·작년 동월 비교 + 월별 막대그래프, 결과보고 화면에도 만족도조사 링크 연결 완료 (`AS기사 대시보드 만족도 점수 현황 추가 및 결과보고 만족도 조사 링크 연결 완료`)
* 담당 화면(기사 대시보드, 결과보고, 만족도설문, 로그인·랜딩) CSS/HTML 마무리 작업 3단계 진행:
  1. `대시보드 디자인 수정_v1` — 레이아웃 구조 정리
  2. `대시보드 및 결과보고 세부 css 작업` → `css 및 html 작업 완료`
  3. AI 도구를 활용한 디자인 고도화: ChatGPT로 초안을 잡고(`gpt css 적용_v1`) → Claude(AI 코딩 어시스턴트)와 함께 담당 화면 전체를 하나의 톤(차콜네이비 + 일렉트릭블루 + 오렌지)으로 재정리하고 로고/히어로 이미지를 새로 제작(`css 클로드 작업_v1`, `세부 css 작업 중_v1`) — 이 과정에서 `reset.css` 누락(404), `join.css` 링크 태그 오타처럼 화면에 안 보이던 잠재 버그도 함께 발견·수정
* **팀 전체**: 같은 날 다른 팀원(ung-Jin)이 전체 페이지에 적용하는 별도의 대규모 리디자인(`AIR LINK` 테마, PR `css/gpt`)을 진행해 merge — 이후 전 화면이 이 새 디자인 시스템으로 다시 통일됨. 캘린더 조회·배정 기능과 오류 메시지 처리, admin2 세부 버그 수정도 이 시점에 함께 반영

<br>

## Day 6 (2026-09-15, 오늘 · 진행 중). 팀 디자인 통합 마무리 + 발표 준비

목표: 팀 전체 디자인 통합본 위에서 담당 화면 최종 점검 + 데모/발표 준비

* 팀원들이 올린 CSS PR 4건(`css/schedule`, `css`, `style/admin2-design`, `feature/asresult`) 병합 완료 — 전 화면이 `AIR LINK` 디자인 시스템으로 통일된 상태에서 담당 화면 최종 점검
* 랜딩 화면(`FirstPage.html`)의 role별 "바로가기" 섹션 레이아웃을 실제 사용 흐름에 맞게 여러 차례 다듬음 (기사 role 전용 배치 조정, 제목/카드 간격·정렬 정리)
* 공통 사이드바 하단 영역을 문의처 정보(관리자명·연락처)로 개선해 정보 공백 보완
* **발표 데모 준비**: AS 기사 대시보드가 "오늘 배정된 일정 없음"으로 비어 보이던 문제 해결 — eng01(김기술) 기준으로 오늘 일정 3건(진행예정/진행중/완료 각 1건) + 만족도조사 더미 데이터(작년 동월 비교, 올해 1~9월 추이) 추가
* Cloudflare Tunnel 재오픈 및 `application.yaml`의 공개 접속 주소 최신화 — 실제 휴대폰으로 SMS 수신 → 링크 접속 → 만족도조사 제출까지 전체 플로우 실환경 테스트 준비
* 본 작업일지 및 발표자료 정리

<br>

## 참고 — 프로젝트 범위에서 제외한 부분

* Spring Security 미사용 — `HttpSession` + 자체 인터셉터(`LoginCheckInterceptor`) 기반으로 로그인/권한을 직접 처리
* CSRF 토큰, 별도 로그 표준화 등 정교한 보안·운영 장치는 적용하지 않음 (학습 목적의 단기 프로젝트 범위로 한정)
* 실시간 알림(푸시)은 없음 — 결과 등록 시 SMS 1건 발송이 유일한 알림 수단
* 지도 기반 주소/지역 매칭, 결제 기능 없음
* 정교한 모바일 반응형은 미적용, 기본 반응형(사이드바 접힘 등) 수준만 적용
